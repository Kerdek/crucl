fold where (

fold list = {
  r g b: rec
    where (
      rec s = list s nonempty b,
      nonempty x y = g x (rec y)),

  l g: rec
    where (
      rec b s = list s nonempty b
        where (
          nonempty x y = rec (g x b) y)) })