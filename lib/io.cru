{ return, bind, do, print, put, process } where (

pre = #"pre.cru",
format = #"format.cru",

cons = pre.cons,
car = pre.car,

return x = ["return", x],
bind x y = ["bind", x, y],
do x y = bind x (car y),
put x = ["put", x],
print x = put (format x))